# MAVEB Discovery Campaign State

Updated: **2026-09-20 18:37 IST**

## Current center of gravity

The campaign has moved away from broad "persistent Gaussian world" claims toward **properties that can be proved or measured precisely**.

### S1 — dependency-certified heterogeneous minimal-work repair
Currently the strongest systems direction. The key observation is that a pipeline can be semantically local while still doing global work. MAVEB already contains exact local TSDF→mesh repair, but PR #28 leaked locality through full Gaussian scans, all-frame publication quiescence, full temporal-history invalidation, and all-resident-block CPU transfer.

Synthetic spatial-index probes have passed sufficiently to justify production-shaped integration.

### S2 — certified non-target geometric preservation
Still alive, but only if it provides a stronger contract than contemporary selective editors: an explicit geometry quantity outside Ω and a useful bound/certificate.

### S3 — temporal representation migration
Static regional mesh/Gaussian assignment is now considered occupied. This survives only as a *temporal* decision problem: a region changes representation across revisions because evidence, update rate, view dependence, memory, or latency regime changes. An offline measured oracle is now committed as the kill gate.

### S4 — resampling-stable lineage
Persistent IDs alone are occupied. This survives only if correspondence remains meaningful through split/merge/prune/densify and yields measurable downstream benefit.

## Important negative result

Raw anisotropic opacity-weighted Gaussian spectral phase has been rejected as a geometry-only invariant. Canonicalized center phase is retained only as a narrow correspondence-free/resampling hypothesis and is no longer treated as a flagship direction.

## Integration state

`research/maveb-discovery-persistent` contains a production-shaped Gaussian spatial index and indexed selector preserving PR #28 ownership semantics. Two inherited PR #28 warning-as-error failures have already been repaired on the experiment branch. Integration CI is still pending and no implementation claim is allowed until it passes.

## Rule

Every future result must either:
- move a shortlist item toward a hard gate,
- kill/pivot it,
- or improve the evidence infrastructure.

No feature work for its own sake.
